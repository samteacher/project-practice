server:
  port: 1115

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/samteacher/eureka-config.git
          search-paths: respo
          username:
          password:
      label: master
